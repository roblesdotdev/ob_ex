const name = 'John'
const lastname = 'Doe'

const student = name.concat(' ', lastname)

const studentUpper = student.toUpperCase()

const studentLower = student.toLowerCase()

const lenStudent = student.length

const first = name.charAt(0)

const last = lastname.charAt(name.length - 1)

const withoutSpaces = student.replace(/ /g, '')

const nameInStudent = student.includes(name)
