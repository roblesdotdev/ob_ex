const heightCM = 180;

const heightM = 1.8;

const weightKG = 90.5;

const heightCeil = Math.ceil(heightM);

const weightFloor = Math.floor(weightKG);

const isSameMax = Number.MAX_VALUE + 1 === Number.MAX_VALUE;

